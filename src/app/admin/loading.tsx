import { LoaderScreen } from '@/shared/components/loader'

/** Loading del backoffice: animación de marca centrada en el área de contenido. */
export default function AdminLoading() {
  return <LoaderScreen />
}
